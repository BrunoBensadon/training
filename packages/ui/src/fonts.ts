/**
 * Self-hosted type. See styles/fonts.css for why the @font-face rules are
 * written out rather than taken from @fontsource's own stylesheets.
 *
 * Import this once, from the app entry point.
 */
import '../styles/fonts.css';
